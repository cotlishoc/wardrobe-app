meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirementsPath: backend/requirements.txt

run:
  persistenceMount: /data
  command: cd backend && uvicorn app.main:app --host 0.0.0.0 --port 80

container:
  ports:
    - 80